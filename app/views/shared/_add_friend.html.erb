  <% if current_user.accepted_friendships.include?(user.id) %>
    <%= button_to "Remove friend", friendship_path(id: user.id), :method => :delete , class: 'btn-primary'%>
  <% elsif  current_user.sent_friend_requests.include?(user.id) %>
    Already sent friend request
  <% elsif  current_user.received_friend_requests.include?(user.id) %>
    <%= button_to "Accept friend request", friendships_path(friend_id: user.id), :method => :post, class: 'btn-primary' %>
  <% else %>
    <%= button_to "Add friend", friendships_path(friend_id: user.id), :method => :post, class: 'btn-primary' %>
  <% end %>



