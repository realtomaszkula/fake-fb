<div class="col-sm-6 col-sm-offset-3">
  <%= render partial: 'shared/post/post_form', locals: { user: current_user } %>

  <% @posts.each_with_index do |p, i| %>
    <%= render partial: 'shared/post/post', locals: { p: p, i: i } %>
    <div class="short-spacer"></div>
  <% end %>
</div>

