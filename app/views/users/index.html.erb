<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
  <li>
    <%= link_to user.name, user_path(user) %>
    <%= render partial: 'shared/add_friend', locals: { user: user } %>
  </li>
  <% end %>
</ul>